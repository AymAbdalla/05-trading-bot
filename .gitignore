# Secrets
.env*
!.env.example

# Database
db/trading.db
db/trading.db-wal
db/trading.db-shm

# Backups
backups/

# Logs
logs/

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# macOS
.DS_Store

# Large graveyard JSON files (>100MB, GitHub limit)
research/graveyard/v0_graveyard_full.json
research/graveyard/archive/
# Sweep outputs and their baselines. Same reason, and one more: a sweep in
# progress is writing its file, so committing it captures a torn artifact.
# 80M and 371M as of 2026-08-18. Promotion is a deliberate `mv`, not a commit.
research/graveyard/v0_graveyard_R005_R006.json
research/graveyard/v0_graveyard_pre_R005.json
research/graveyard/v0_graveyard_full_fix2.json

# Superseded by silent_assertions block in judge_evidence_pack.json
research/graveyard/assertions.json

# Pytest cache
.pytest_cache/

# Internal agent files - never public
CLAUDE.md
HANDOVER.md
**/SOUL.md
docs/handoffs/from-raven/
docs/handoffs/from-cody/

# Internal agent directory - installed subagents, session settings. Not a
# portfolio artifact. `.claude/agents/forge.md` asserts this in its install
# comment, so the assertion has to be true (Raven ruling, 2026-08-18).
.claude/

# Cloned third-party reference repos - read for ideas, never our code
research/moondevonyt-ai-trading-battles/
research/moondevonyt-polymarket-examples/
trading_bot.db

# Third-party repos cloned for study 2026-08-18. Not ours, not vendored.
research/cloddsbot/
research/mrfadiai-polymarket-bot/
research/polymarket_lp_tool/

# Fetched open-meteo / METAR history for
# backtest/measure_daily_extreme_calibration.py. ~15MB of raw API
# responses, regenerable by re-running the harness. The FIT it
# produces (research/weather_sigma_calibration.json) is tracked; the
# bytes it was fitted from are not.
backtest/data/weather_calibration_cache/

# D-319: live runtime artifacts, untracked 2026-08-18. Written continuously
# by running processes (shadow loop, hyperliquid poller, validate_harness),
# so a commit only ever captures a torn snapshot - and the paper log had
# reached 419MB on disk, past GitHub 100MB push limit. Working copies stay
# on disk; the loop and the harness read them live.
research/graveyard/harness_validation.json
research/hyperliquid/leaderboard_wallets.json
research/polymarket_paper/polymarket_paper_log.csv
db/trading-survivors.db
db/trading-survivors.db-wal
db/trading-survivors.db-shm
research/polymarket_paper_survivors/
