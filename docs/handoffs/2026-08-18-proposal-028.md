# Proposal 028: PM_status_quo_collector + status_quo_classifier

**Raven, 2026-08-18.** Written from verification of the tree (the build session died before its epilogue; the classifier-fix session's handoff covers the fix). Committed as `57a90f2`, pushed.

## What shipped

| File | What |
|---|---|
| `strategies/polymarket/status_quo_classifier.py` | Deterministic, rule-based, shape-based classifier: STATUS_QUO / CHANGE_EVENT / UNKNOWN. Honest UNKNOWN default that never trades. Classifies question SHAPE not probability (the exact trap the reference wallet's $93k loss came from, documented in the module). No model calls. |
| `strategies/polymarket/status_quo_collector.py` | The tail-risk seller: NO side only, 0.80-0.90 band, dated binary political questions, fixed small size capped so one position going to zero loses <5% of bankroll, scale-up ladder at 82c/89c, never above 0.90, hold to resolution, stop 0.00. Ladder rung state tracked per market slug. |
| `tests/test_status_quo_classifier.py` + `test_status_quo_collector.py` | Classifier cases (STATUS_QUO / CHANGE_EVENT / UNKNOWN incl. the regime-change trap), strategy gates, band, ladder, size cap. |
| `docs/DECISIONS.md` | D-318 (RATIFIED by Raven): smart_money_callers fixed 5-share sizing, Kelly deferred behind a resolution oracle. |

## Verification

- Full suite: **3,707 passed, 1 skipped, 0 failed** (verified by the classifier-fix session's own run; the fix was the only failing test before it).
- Harness: 21/21, exit 0.
- Classifier semantics: 8 new skip reasons classified per the file's own test (missing input = DATA_BLOCKER, false condition = GENUINE) — see `2026-08-18-proposal-028-classifier-fix.md`.

## Status

- Registry: **22** (status_quo_collector appended). Declares the political space only.
- Strategy is WIRED as a document — the loop runs PRE-028 source (PID 3108 snapshot at import, Convention 13), so it will not evaluate until the next loop restart, which is Aym's call.
- The classifier is the whole edge and the whole risk (per the proposal) — it passed its own tests but has zero real political-market volume behind it. NOT_TESTED until it accumulates resolutions.

## Queue after this

- 032 (longshot_fade) → 033 (weather bracket) → 034 (fair-value settlement exit, preconditioned on stop_px vs price_stop). All drafted in from-raven/.

## Rules

- Shadow only. Never live. No backtesting.
- Loop NOT restarted (Aym's call).
