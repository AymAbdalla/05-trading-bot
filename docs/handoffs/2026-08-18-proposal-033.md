# Proposal 033: PM_weather_bracket_width_matched

**Raven, 2026-08-18.** Written from verification of the tree (the build session died before its epilogue; the fix session's handoff `2026-08-18-proposal-033-fix.md` covers the fix). Committed as `b1b8652`, pushed.

## What shipped

| File | What |
|---|---|
| `strategies/polymarket/weather_bracket_width_matched.py` (31KB) | The 3-rung bracket strategy: buys a contiguous 3-rung basket (effective width 5.4F) matching the model's 2.74F RMSE — the third option beyond "fit sigma" and "lower the floor". 24-48h lead only, mu from the point forecast, sigma from config (refit propagates), p_bracket vs cost gates (>=0.04 edge, <=0.85 cost), 3 legs entered in one poll cycle (thinnest first), partial-fill → immediate leg_risk_unwind (proposal 026 rule 7), hold to resolution, 25 skip reasons. |
| `tests/test_weather_bracket_width_matched.py` (30KB) | 52 tests: bracket construction, p_bracket math, gate logic, lead band, partial-fill unwind, registry pin. |
| `agents/forge_shadow_eval.py` | The `daily_extreme_estimate` failure path fixed with an explicit 10-branch enumeration (the AST resolver can't follow a status variable; a collapsed single reason broke 033's own existing tests — verified from source, not the brief's guesses). |

## Verification

- Full suite: **3,788 passed, 1 skipped, 0 failed** (verified by the fix session's run).
- Harness: 21/21, exit 0.

## Status

- Registry: **24** strategies (033 appended at index 23). Declares weather space.
- Loop runs PRE-027 source (PID 3108, Convention 13) — will not evaluate 027/028/032/033 until the next restart, which is Aym's call.
- NOT_TESTED until it accumulates 40+ resolved brackets (kill condition: net below 0 or >40% of outcomes outside the bracket = the fitted sigma is understated).

## Queue after this

- 034 (fair-value settlement exit, preconditioned on the stop_px vs price_stop contradiction — the blocking task 0 is the first thing the implementation must do).
- Then: loop restart brings all 24 strategies live, Forge fires on the new data.

## Rules

- Shadow only. Never live. No backtesting.
- Loop NOT restarted (Aym's call).
