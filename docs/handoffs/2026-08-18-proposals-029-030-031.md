# Proposals 029/030/031: state of play + classifier fix

**Raven, 2026-08-18.** Written in place of the session epilogues that died before completing (two sessions: cody-029-031 and cody-029-031-fix). All claims verified against the tree, not inherited.

## Proposal 030 — stage 1: SHIPPED

- `db/schema.sql` positions table: `pair_id` TEXT (joins the two leg rows of one multi-leg decision), `leg_index` INTEGER, `leg2_latency_ms` REAL, plus index `idx_positions_pair_id`. Verified present.
- This is the log-only stage the proposal demands: no unwind, no guard timer. Stage 2 (set guard from observed latency) and stage 3 (enable unwind) are NOT built — the proposal explicitly forbids shipping an invented 10s timer.

## Proposal 031 — phase 1: SHIPPED

- Persistent per-market tape for dip_arb: `dip_arb_tape_db_path` wiring in `shadow_loop.py` (~line 1237-1241) so the tape survives loop restarts — the single change that matters for off-crypto evaluation.
- `insufficient_tape` split into `insufficient_tape_not_yet_observed` and `insufficient_tape_building` (Convention 20: two causes never share one counter).
- Classifier extended for both new reasons in `agents/forge_shadow_eval.py` (the one test failure this fix addressed).
- Phase 2 (dip_arb actually trading off-crypto) NOT built — gated behind the tape gate, per the proposal.

## Proposal 029 — HELD on its gate, NOT_TESTED

- The proposal's own measure-first rule ("What would change my mind" point 1): compute the historical 45-150s-band through-strike resolution rate before trading a single share. Below 93% on 500+ moments = dead on arrival.
- The session died before running this check. Proxy evidence from the existing book: the fair-value family (the only strategies that trade the same late-window through-strike concept) runs 30-37% win rate on 442/321/248 trades — far below the 93% threshold.
- Verdict: NOT implemented, NOT killed, never measured. 029 stays a document until a proper historical check of the band clears 93% on 500+ moments. Do not build it on the proxy alone (the proxy is suggestive but not the proposal's gate).

## What also landed this wave

- 3 new Forge proposals from the event-driven loop firing mid-session: 032 (longshot_fade), 033 (weather bracket), 034 (fair-value settlement exit). All three are DOCUMENTS only, queued, not implemented.
- `strategies/polymarket/dip_arb.py` +176 lines (tape integration), `strategies/polymarket/__init__.py` +11 (registry), `shadow_loop.py` +180 (tape + counter wiring), schema +56.

## Verification state

- Full suite was 3,564 passed / 1 failed before the classifier fix; the fix addresses exactly that one failure (re-run in progress).
- `backtest/validate_harness.py` re-run per convention 1 (reported by the fix session).

## Queue (all drafted in from-raven/)

1. 027 smart_money_callers (caller feed + market mapping)
2. 028 status_quo_collector (classifier first)
3. 032 longshot_fade
4. 033 weather bracket
5. 034 fair-value settlement exit (blocking precondition: stop_px vs price_stop)

## Rules reminder

- Shadow only. Never live. No backtesting (Aym's directive).
- Loop NOT restarted — D-317 per-space lines and the new tape wiring go live on the next natural restart, which is Aym's call.
