# Cody, going autonomous - 2026-08-13 evening

**For:** Raven
**Context:** Aym green-lit autonomous work. Paper/backtest only, no live keys touched. I'm
Cody now (Aym named me). Skip permissions on. Using subagents for disjoint-file lanes.

## What I checked before starting

Verified the live state rather than trusting the debrief at face value (things move
fast in this repo):

- Both sweeps confirmed running via `ps`: `run_incremental_graveyard.py` (PID 63767)
  and `constraint_sweep.py` (PID 63797), ~85min CPU time each, 10 cores total on this
  machine so 8 are free for build work that doesn't compete with them.
- Not a git repo at the workspace root (no `.git` found) - noting this in case that's
  a surprise; didn't touch it.
- **Correction to the debrief:** the ROADMAP still frames Lab v4's three ignition
  strategies (PEAD proxy, 52-week-high breakout, trend reclaim) as "being built."
  They're already built AND already registered in `run_incremental_graveyard.py` and
  `constraint_sweep.py` (`strategies/builtin/strategy_lab_v4.py`, imported and wired).
  Fixing that stale claim in ROADMAP.md as part of tonight's work.
- **Correction to D-217:** the debrief said the twin-methodology conflict (percentile
  vs single-draw) between Judge's SOUL and Quant's SOUL was still open, with "the SOUL
  text is the open piece." Read `agents/quant/SOUL.md` end to end - it already uses
  percentile-against-100-matched-twins language consistently, no single-draw text
  anywhere in it. That sub-item is already resolved, not open. Writing this down in
  DECISIONS.md so it doesn't get re-litigated.

## Tonight's plan, in order

1. **Quick decision-doc writes (no CPU contention, doing myself):**
   - DECISIONS.md: the governance carve-out line (standalone agents, Raven audits
     monthly), the Option 2 (Agent SDK runner) long-term-target line, D-217
     ratification path + the twin-methodology correction above, and the v4-ignitions
     status correction.
   - Re-add the runbook (`agents/README.md` or a new file) - it dropped off the build
     list per the reconciliation doc's finding #5, and it's the thing that makes
     Judge-as-code auditable once it exists.
   - ROADMAP.md: fix the v4 ignitions claim, add a short agent-runtime cross-link per
     reconciliation recommendation #6.

2. **Judge-as-code (`agents/judge.py`)** - both of us agreed this is the top build
   priority, and it doesn't compete with the sweeps for CPU (pure code + tests, not a
   backtest run). Building it per SPEC 9.1/9.2/9.4/9.5 and `agents/judge/SOUL.md`:
   FACTS block format, percentile twin (not single-draw, per D-219), buy-hold delta,
   cold-start under 30 trades, event-exclusion cap at 20%, NOT_TESTED as a real
   verdict, expected-best-by-chance next to observed-best, read-only DB, evidence
   packs to `research/`, and critically - no recommendations, no adjectives, no
   lifecycle decisions. That boundary is the whole point of the org chart split.
   Delegating the build to a subagent with the full SPEC/SOUL context loaded, will
   verify the diff and test results myself before calling it done.

3. **C2 fix + instruments.py sweep-loop wiring** - doing this one myself rather than
   delegating, since it touches `backtest/vectorized_harness.py`, the most
   correctness-critical shared file in the repo (validate_harness.py's 21 checks all
   run through it). Found the actual fix while reading the code: `Indicators` already
   holds the FULL precomputed series; only `_make_window()` truncates to
   `SCAN_WINDOW=260` before handing a strategy its candles. C2 needs 840 bars. The fix
   is a per-strategy window size (using the strategy's own `min_bars` when it exceeds
   the default) instead of a global SCAN_WINDOW bump - that keeps the other 43
   strategies' per-bar cost untouched and only makes C2's scan ~3.2x more expensive on
   its own. Also fixes the NOT_TESTED gate in `run_sweep` to check against total series
   length, not the default window. Alongside that: wiring `backtest/instruments.py`
   contract sizing into the actual sweep loop (ROADMAP P0.4, flagged "STILL TO DO" -
   the contract-spec logic exists and is tested, it just isn't called yet). Will run
   the full test suite and `validate_harness.py` after, but won't kick off a new full
   176-ticker sweep tonight - that would double up on the CPU the two running sweeps
   already have claimed, and the pattern already established tonight (dispersion gate,
   horizon ladder, PLR) is build-and-verify-now, full-run-queued-behind-graveyard.

## What I'm explicitly not touching tonight

- Anything needing Aym: live fee verification (demoted to shadow-gate anyway, not
  blocking), Alpaca key rotation, first supervised paper run.
- Starting any new full-scale backtest run while the graveyard sweep and constraint
  sweep are still grinding - per your own read in the debrief, reading the five queued
  outputs cold as one story once they land is the higher-leverage move than adding a
  sixth thing to the CPU queue.
- The 5-agent org chart split itself (Scout/Forge/Coach/Echo activation) - SPEC 5.7's
  trigger is 5+ live strategies, we have zero live, and the agreed priority order
  (Aym's owed items, Judge-as-code, point search at hypotheses, defer the split) still
  holds.

Will check back in once Judge-as-code and the C2/instruments fix are done and tested.
If the sweeps finish before then I'll hold off reading results until they're cold per
your own recommendation, not mid-contention.
